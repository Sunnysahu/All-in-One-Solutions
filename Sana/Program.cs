using System;
using System.Collections.Generic;
using System.Linq;

namespace Sana
{
    internal class Program
    {

        public static void Main(string[] args)
        {

            int[] numbers = { 1, 2, 3, 4, 5, 6};

            // int long decimal bool, double, ulong, byte, ushort, short

            //var result = numbers.Select(sana => sana * 100);

            //foreach (int i in numbers.Where(x => x % 2 == 0).Select(sana => sana * 100))
            //{
            //    Console.WriteLine(i);
            //}
        }

    }
}